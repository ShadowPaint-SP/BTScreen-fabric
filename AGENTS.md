# Introduction

This repository is a GUI to control Baritone and use it for various tasks.

## Implementation Guidance

- never build regression Tests. In general this Projekt doesnt use JUnit testing we test by playtesting.
- At the end of an interaction always run a `./gradlew build` if a code change happened